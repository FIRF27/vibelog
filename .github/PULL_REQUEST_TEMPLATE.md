## What & why

<!-- What does this change and why? Link any related issue. -->

## Checklist

- [ ] `npm test` passes
- [ ] `npm run build` is clean
- [ ] If `src/action`, `src/core`, or `src/cli` changed: ran `npm run build:action` and committed `dist-action/`
- [ ] Added/updated tests for the behavior
